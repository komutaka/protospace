$(function() {

  $('#new-main-image input:file').uploadThumbs({
  });
  $('#new-sub1-image input:file').uploadThumbs({
  });
  $('#new-sub2-image input:file').uploadThumbs({
  });
  $('#edit-main-image input:file').uploadThumbs({
  });
  $('#edit-sub1-image input:file').uploadThumbs({
  });
  $('#edit-sub2-image input:file').uploadThumbs({
  });
  $('#new-user-avatar input:file').uploadThumbs({
  });
  $('#edit-user-avatar input:file').uploadThumbs({
  });

});
